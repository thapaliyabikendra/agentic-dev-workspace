# Normalize line endings: LF in the repository, native on checkout.
# Added after the 2026-06-10 develop merge, where CRLF/LF divergence between
# branches turned every shared file into a whole-file merge conflict.
* text=auto

# Shell scripts run inside the Linux devcontainer; CRLF breaks bash.
*.sh text eol=lf
